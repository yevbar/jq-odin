# Specialty behavior workstream

Inventory regex, modules, codecs, time, locale, and optional platform behavior.

Primary upstream surfaces include `tests/onig.test`, `tests/manonig.test`,
`tests/base64.test`, `tests/uri.test`, `tests/modules/`, `tests/man.test`, and
`tests/optional.test`.

Do not create implementation packages until their dependency direction and
foreign-library policy are accepted.

