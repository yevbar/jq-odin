# CLI workstream

Inventory argv, stdin/stdout/stderr bytes, exit status, environment, module
paths, colors, raw modes, streaming, broken pipes, and diagnostics from
`upstream/jq/tests/shtest`.

Own CLI compatibility cases now. Creation of an Odin driver or command package
waits for one integrated compile/evaluate vertical slice.

