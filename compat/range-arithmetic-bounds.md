# Arithmetic range bounds

`range` accepts safe literal arithmetic bounds and emits the expected
fractional sequence. This isolates the arithmetic-bound portion of the broad
upstream probe at `upstream/jq/tests/jq.test:2029`.
