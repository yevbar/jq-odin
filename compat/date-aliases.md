# Date aliases

This shard covers jq's `fromdateiso8601`/`fromdate` and
`todateiso8601`/`todate` aliases using the bounded UTC ISO implementation.

Oracle evidence: `upstream/jq/src/builtin.jq:73-76`.
