# Negative literal skip/nth counts

This shard covers unary-negative literal counts for `skip` and `nth`, including
their catchable jq diagnostics. Dynamic counts and broader generator forms are
outside this bounded slice.
