interface:
  display_name: "Handle jq Rewrite Task"
  short_description: "Launch an isolated Vers task agent"
  default_prompt: "Use $handle-task to launch and supervise an isolated jq rewrite task."
