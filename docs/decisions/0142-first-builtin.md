# Decision 0142: zero-argument first selector

`first` is an append-only operand-free opcode returning the first array
element, or null for empty/null inputs. Generator forms and exact diagnostics
remain deferred.
