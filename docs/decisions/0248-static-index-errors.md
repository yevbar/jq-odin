# Static index assignment runtime errors

Static array assignment now maps out-of-range and oversized indices from the
value layer to jq-compatible catchable runtime messages. Valid assignment
semantics and path syntax are unchanged.
